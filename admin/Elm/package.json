{
    "private": true,
    "dependencies": {
        "elm": "^elm0.19.0",
        "elm-format": "^0.8.1",
        "elm-test": "^0.19.0"
    },
    "scripts": {
        "build": "elm make --optimize --output=../dist/index.html src/Main.elm",
        "test": "elm-test",
        "format": "elm-format ."
    }
}