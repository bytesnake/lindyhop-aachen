{
    "private": true,
    "dependencies": {
        "elm": "^elm0.19.0",
        "elm-format": "^0.8.1",
        "elm-test": "^0.19.0"
    },
    "scripts": {
        "build": "elm make --optimize --output=./dist/index.html src/Main.elm",
        "test": "elm-test",
        "format": "elm-format ."
    },
    "devDependencies": {
        "chokidar-cli": "^1.2.2",
        "concurrently": "^4.1.0",
        "json-server": "^0.14.2"
    }
}