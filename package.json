{
    "private": true,
    "scripts": {
        "install": "(cd admin && yarn install)",
        "build": "(cd admin && yarn build)",
        "test": "(cd admin && yarn format --validate && yarn test)"
    }
}